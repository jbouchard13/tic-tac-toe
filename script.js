// create a module for the gameboard
const GameBoard = (() => {
  // gather the 9 boxes and put them into an array
})();

// create a module for game controls
const Controller = (() => {})();

// create a factory for player objects
const Player = (playerName) => {};
